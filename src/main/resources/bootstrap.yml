spring:
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: short-link
        username: nacos
        password: nacos
        file-extension: yaml
        group: DEFAULT_GROUP
        refresh-enabled: true
      discovery:
        server-addr: localhost:8848
        namespace: short-link
        username: nacos
        password: nacos
        group: DEFAULT_GROUP